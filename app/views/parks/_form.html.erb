<%= bootstrap_form_for(park) do |f| %>
  <% if park.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(park.errors.count, "error") %> prohibited this park from being saved:</h2>

      <ul>
      <% park.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.text_field :name %>
  <%= f.text_area :description %>
  <%= f.file_field :picture %>
  <div class="well">
    <h3>Rangers</h3>
    <%= f.fields_for :rangers do |ranger_form| %>
      <div>
        <%= ranger_form.text_field :name %>
        <% unless ranger_form.object.new_record? %>
          <%= ranger_form.check_box :_destroy %>
        <% end %>
      </div>
    <% end %>
  </div>

  <%= f.submit %>
<% end %>
