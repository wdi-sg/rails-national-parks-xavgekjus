<div id="parks">
  <h1>All Private Parks</h1>


  <div class="row">

    <% @parks.each_with_index do |park,index| %>
    <!-- <pre><%= park.inspect %> </pre> -->
      <div class="col-md-4">
        <!-- <img class= "parkPics" src="<%=park.picture%>">  SHORTCUT BELOW -->
        <%= image_tag(park.picture)%>
        <p><%= link_to park.name, park_path(park.id), class: "btn btn-info"%></p>
        <br>

      </div>
    <%end %>

  </div>
  <p>
    <%= link_to("Home", root_path, class: "btn btn-primary")%>
    <%= link_to("New Park", new_park_path, class: "btn btn-success")%>
  </p>
</div>
